/**
 * VideoMetadataWidget view component.
 *
 * Renders nothing — metadata is consumed programmatically, not displayed.
 */
import React from 'react';

const VideoMetadataWidgetView: React.FC = () => {
  return <></>;
};

export default VideoMetadataWidgetView;
